---
import type { ITechnology } from "@/types";

import Card from "@/components/cards/Card.astro";
import RichImage from "@/components/RichImage.astro";
import TechIcon from "@/components/TechIcon.astro";

interface Props {
  title: string;
  description: string;
  technologies: Array<ITechnology>;
  imageSrc?: string;
}

const { title, description, technologies, imageSrc } = Astro.props;
---

<Card tag="article" class:list={["group/project-card flex items-center"]}>
  <RichImage src={imageSrc} class="shrink-0 w-[40%] h-full object-cover" />
  <div class="p-6">
    <h1 class="font-bold">{title}</h1>
    <div class="flex flex-wrap gap-1">
      {technologies.map((tech) => <TechIcon name={tech.name} class="w-4" />)}
    </div>
    <p class="text-sm">{description}</p>
  </div>
</Card>
