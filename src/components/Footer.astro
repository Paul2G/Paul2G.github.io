---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<footer
  class={`w-full flex justify-center px-4 py-8 bg-surface-50 text-contrast border-t-2 border-t-surface-950 ${className}`}
>
  <div class="container w-full flex justify-center">
    <span class="text-center">Hola</span>
  </div>
</footer>
