---
import type { TechName } from "../types";

interface Props {
  title: string;
  description: string;
  technologies: Array<TechName>;
  imagePath?: string;
}

const { title, description, technologies, imagePath } = Astro.props;
---

<article>
  <h1>{title}</h1>
</article>
